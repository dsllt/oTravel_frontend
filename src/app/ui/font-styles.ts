import { Inter, Roboto } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const roboto_400 = Roboto({ subsets: ['latin'], weight: '400' });
export const roboto_700 = Roboto({ subsets: ['latin'], weight: '700' });

